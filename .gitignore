# Vendored npm dependencies — restored by build.sh / scripts/setup.sh.
# Tracked separately via package.json + package-lock.json.
skills/sidecar/proxy/node_modules/

# macOS metadata
.DS_Store
**/.DS_Store

# Build artifacts
*.plugin
build/
dist/

# Editor caches
.vscode/
.idea/
*.swp
*.swo

# Local Claude/Cowork session artifacts — but settings.json and rules/ are
# tracked (shared harness config installed by harness-engineering:setup).
.claude/*
!.claude/settings.json
!.claude/rules/
.clearance-rendered-preview-*/

# Pre-push check cache (SHA of last HEAD that passed full checks)
.checks-passed

# esbuild bundle (regenerated by build.sh)
skills/sidecar/proxy/bundle.cjs
