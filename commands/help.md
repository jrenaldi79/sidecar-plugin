---
description: Show what Sidecar can do — capability tour with example prompts and current status
---

Load the `sidecar` skill and follow its **Help mode** section: present the capability tour (one example phrase per feature, in user-friendly language), then run the skill's Step 0 config check and report whether Sidecar is configured. If it isn't, offer to run setup; if it is, suggest a first ask tailored to what the user is currently working on.

Do not duplicate the tour content here — the canonical table lives in the skill's Help mode section.
